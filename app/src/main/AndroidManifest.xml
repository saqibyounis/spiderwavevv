<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spiderwave.activities"
    >

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginByPhonenumber"></activity>
        <activity android:name=".OTPActivity" />
        <activity android:name=".CheckRewards" />
        <activity android:name=".Loagastics" />
        <activity android:name=".LoanActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignupPhase2" />
        <activity android:name=".SignupPhase1" />
        <activity android:name=".SubmitITR" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>